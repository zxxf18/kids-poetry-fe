# 独立域名部署

应用从站点根路径提供页面、图像及 `_next` 资源，API 使用 `/api/v1`。Nginx 将 `/api/` 原样转发至后端，其余请求转发至前端，不再添加旧应用前缀。

Docker 健康检查使用 `/`。根路径构建无需复制带前缀的静态资源目录。发布后验证首页、诗歌详情、返回列表、音频 API 和静态资源；浏览器缓存及阅读位置按 origin 隔离，不会自动迁移到新域名。
